# Python bytecode
__pycache__/
*.py[cod]

# Local dev reference — a copy of the standalone poller (its own repo:
# jiwanus/inpost-poller). Kept locally for reference, not part of this repo.
reference-poller/
